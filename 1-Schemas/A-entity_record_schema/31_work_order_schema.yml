$schema: 'http://json-schema.org/draft-07/schema'
title: workOrder
$id: workOrderProject
type: object
properties:
  
  failure report(s):
  

note: |
  WORK IN PROGRESS
  - ususally WO are issued against roles, but it must also be easy to issued against tools other rotating assets (even if they are in storage)
  - Cannot issue work order to an asset unless it's status is "in hand / usable" or "abondoned in place"
  - Cannot issue work order t`o a role or a space unless it's status is "realized"