title: organization
$id: orgGroupObject
$schema: 'http://json-schema.org/draft-07/schema'
type: object

properties:

  organization ID:
    type: string
    description: 
    
  organization name:
    type: string
    description:

  alias name(s):
    type: string
    description:

  parent organization:
    type: string
    description:

  leader:
    # do not need "track: true" as this information should be tracked in the metadata DB

  location: #should this be service address
    type: string

  #========================

  duplicate record of:
    oneOf: 
      - type: array
        items:
          $ref: orgGroupObject
      - type: null
  
  #========================
  allOf:
    - $ref: genericEntityObject
