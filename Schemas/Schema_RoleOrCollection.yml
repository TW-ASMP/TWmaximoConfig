title: role
$id: objectDefs/roleOrCollection
$schema: 'http://json-schema.org/draft-07/schema'
type: object

$comment: >
  to self: role must have a location

properties:
  roleNumber:
    type: string
    description: 

  roleName:
    type: string
    description: 

  parentRole:
    type: string
    description: 

  roleClass:
    type: string
    description: 

  #clientEntity

  #roleTechnicalRequirementDescription
  
  locatedInSpace:
    type: string
    description: 

  serviceAddress:
    type: string
    description: 

  criticality:
    type: string
    description:
    #[]self: criticality criteria

    # comment: Silvia is missing the descriptions of the 1 to 5 talk to Pete
  
  if:
  oneOf:
    - roleClass:
        properties:
            className:
              const: "pumping station"
    - roleClass:
        properties:
            className:
              const: "water treatment plant"
    - roleClass:
        properties:
            className:
              const: "large chamber"
  then:
    required:
      - 